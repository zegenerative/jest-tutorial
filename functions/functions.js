const functions = {
    mult: (a, b) => {
        console.log('multiplying...')
        return a * b
    },
    speak: (a, b) => {
        console.log('Speaking...')
        return a + b
    }
}

module.exports = functions